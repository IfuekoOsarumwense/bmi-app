services:
  - type: web
    name: bmi-calculator
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bmi_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
